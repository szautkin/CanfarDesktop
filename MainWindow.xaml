<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CanfarDesktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Verbinal - a CANFAR Science Portal">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Grid Grid.Row="0" Padding="16,8" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left: branding + home -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Image Source="Assets/Verbinal_icon.png" Height="28" />
                <TextBlock Text="Verbinal" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
                <Button Click="OnHomeClick" ToolTipService.ToolTip="Home" Padding="6,4"
                        Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE80F;" FontSize="16" />
                </Button>
            </StackPanel>

            <!-- Center: status -->
            <TextBlock Grid.Column="1" x:Name="StatusText" VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Style="{StaticResource CaptionTextBlockStyle}" />

            <!-- Right: auth area -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Button Click="OnAboutClick" ToolTipService.ToolTip="About" Padding="6,4"
                        Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE946;" FontSize="16" />
                </Button>
                <ProgressRing x:Name="AuthProgress" IsActive="False" Width="20" Height="20" />
                <Button x:Name="LoginButton" Content="Login" Click="OnLoginClick" />
                <DropDownButton x:Name="UserButton" Visibility="Collapsed">
                    <DropDownButton.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Logout" Click="OnLogoutClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE7E8;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
        </Grid>

        <!-- Content: 3 overlapping containers, visibility-toggled -->
        <Grid Grid.Row="1">
            <Border x:Name="LandingContainer" />
            <Border x:Name="PortalContainer" Visibility="Collapsed" />
            <Border x:Name="SearchContainer" Visibility="Collapsed" />
            <Border x:Name="ResearchContainer" Visibility="Collapsed" />
            <Border x:Name="StorageContainer" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Window>
