<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="CanfarDesktop.Views.CubeViewer.CubeExportDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Uid="Cube_ExportDialog"
    Title="Export figure"
    CloseButtonText="Close"
    DefaultButton="Close">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1280</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">920</x:Double>
    </ContentDialog.Resources>

    <!-- Sized from the window at open time rather than fixed: a ContentDialog clips content it cannot
         fit rather than shrinking it, and what got clipped here was the bottom of the options column,
         where the export buttons are. MinWidth/MinHeight are the point below which the preview stops
         being worth showing. -->
    <Grid x:Name="DialogRoot" Width="1000" Height="600" MinWidth="520" MinHeight="360" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="264" />
        </Grid.ColumnDefinitions>

        <!-- Off-screen host: a fresh plate is rasterized here at full natural size for export
             (the preview plate is scaled down inside a Viewbox, which would soften the text). -->
        <Canvas x:Name="RasterHost" Grid.ColumnSpan="2" IsHitTestVisible="False" />

        <!-- Live WYSIWYG preview of the figure plate -->
        <Border Grid.Column="0" CornerRadius="6" Padding="14"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1"
                Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Viewbox x:Name="PreviewBox" Stretch="Uniform" StretchDirection="DownOnly" />
        </Border>

        <!-- Style controls. The options scroll; the actions do NOT — a button that scrolls out of
             reach is a button that is not there, and these are the reason the dialog is open. -->
        <Grid Grid.Column="1" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled">
            <StackPanel Spacing="12">

                <StackPanel Spacing="2">
                    <TextBlock x:Uid="Cube_ExpThemeLabel" Text="Theme" Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="ThemeCombo" HorizontalAlignment="Stretch" SelectionChanged="OnStyleChanged">
                        <ComboBoxItem x:Uid="Cube_ExpThemeDark" Content="Cockpit dark" />
                        <ComboBoxItem x:Uid="Cube_ExpThemeLight" Content="Journal light" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Spacing="2">
                    <TextBlock x:Uid="Cube_ExpFontLabel" Text="Font" Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="FontCombo" HorizontalAlignment="Stretch" SelectionChanged="OnStyleChanged">
                        <ComboBoxItem x:Uid="Cube_ExpFontSans" Content="Sans" />
                        <ComboBoxItem x:Uid="Cube_ExpFontMono" Content="Mono" />
                        <ComboBoxItem x:Uid="Cube_ExpFontSerif" Content="Serif" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Spacing="2">
                    <TextBlock x:Uid="Cube_ExpTextColorLabel" Text="Text color" Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="TextColorCombo" HorizontalAlignment="Stretch" SelectionChanged="OnStyleChanged">
                        <ComboBoxItem x:Uid="Cube_ExpColorAuto" Content="Auto" />
                        <ComboBoxItem x:Uid="Cube_ExpColorWhite" Content="White" />
                        <ComboBoxItem x:Uid="Cube_ExpColorBlack" Content="Black" />
                        <ComboBoxItem x:Uid="Cube_ExpColorCyan" Content="Cyan" />
                        <ComboBoxItem x:Uid="Cube_ExpColorAmber" Content="Amber" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Spacing="2">
                    <TextBlock x:Name="ScaleLabel" Text="Text scale 1.00×" Style="{StaticResource CaptionTextBlockStyle}" />
                    <Slider x:Name="ScaleSlider" Minimum="0.75" Maximum="1.5" StepFrequency="0.05" Value="1.0"
                            ValueChanged="OnScaleChanged" />
                </StackPanel>

                <ToggleSwitch x:Name="AnnotateToggle" x:Uid="Cube_ExpAnnotateToggle" Header="Annotations" IsOn="True"
                              OffContent="Off" OnContent="On" Toggled="OnStyleChanged" />
                <ToggleSwitch x:Name="MarksToggle" x:Uid="Cube_ExpMarksToggle" Header="Marks" IsOn="True"
                              Toggled="OnStyleChanged" />
                <ToggleSwitch x:Name="TransparentToggle" x:Uid="Cube_ExpTransparentToggle" Header="Transparent background"
                              OffContent="Off" OnContent="On" Toggled="OnStyleChanged" />

                <StackPanel Spacing="2">
                    <TextBlock x:Uid="Cube_ExpResolutionLabel" Text="Resolution" Style="{StaticResource CaptionTextBlockStyle}" />
                    <ComboBox x:Name="ResCombo" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="2×" />
                        <ComboBoxItem Content="4×" />
                    </ComboBox>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

            <StackPanel Grid.Row="1" Spacing="8">
                <Button x:Uid="Cube_ExpPngButton" Content="Export PNG…" Click="OnExportPng" HorizontalAlignment="Stretch"
                        Style="{StaticResource AccentButtonStyle}" />
                <Button x:Uid="Cube_ExpPdfButton" Content="Export PDF…" Click="OnExportPdf" HorizontalAlignment="Stretch" />

                <TextBlock x:Name="StatusLabel" TextWrapping="Wrap"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>
