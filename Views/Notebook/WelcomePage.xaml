<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CanfarDesktop.Views.Notebook.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                MaxWidth="500" Padding="40" Spacing="24">

            <!-- Logo + title -->
            <FontIcon Glyph="&#xE70B;" FontSize="48"
                      Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                      HorizontalAlignment="Center" />
            <TextBlock Text="Jupiter" Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Native Notebook Editor"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />

            <!-- Action buttons -->
            <StackPanel Spacing="8" HorizontalAlignment="Stretch" MinWidth="400">
                <Button Click="OnNewNotebook" HorizontalAlignment="Stretch"
                        Style="{StaticResource AccentButtonStyle}" Padding="16,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8A5;" FontSize="16" />
                        <TextBlock Text="New Notebook" />
                    </StackPanel>
                </Button>
                <Button Click="OnOpenNotebook" HorizontalAlignment="Stretch" Padding="16,10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8E5;" FontSize="16" />
                        <TextBlock Text="Open Notebook" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Recent files (scrollable) -->
            <StackPanel x:Name="RecentSection" Spacing="8" Visibility="Collapsed" MinWidth="400">
                <TextBlock Text="Recent Notebooks"
                           Style="{StaticResource SubtitleTextBlockStyle}" />
                <ScrollViewer MaxHeight="400"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="RecentList" Spacing="4" />
                </ScrollViewer>
            </StackPanel>
    </StackPanel>
</UserControl>
