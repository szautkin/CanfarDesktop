<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CanfarDesktop.Views.Controls.PlatformLoadControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CanfarDesktop.Views.Controls">

    <Border Padding="16" CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
        <StackPanel Spacing="12">
            <!-- Header -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Platform Load" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Button Grid.Column="1" Click="OnRefreshClick" ToolTipService.ToolTip="Refresh">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                </Button>
            </Grid>

            <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Visibility="Collapsed" />

            <!-- Metric bars -->
            <controls:MetricBar x:Name="CpuBar" Label="CPU Cores" Unit="cores" />
            <controls:MetricBar x:Name="RamBar" Label="RAM" Unit="GB" />

            <!-- Last updated -->
            <TextBlock x:Name="LastUpdateText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" />

            <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error" Title="Error" />
        </StackPanel>
    </Border>
</UserControl>
