<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CanfarDesktop.Views.Controls.RecentLaunchesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Padding="16" CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
        <Grid RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recent Launches" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Button Grid.Column="1" x:Name="ClearButton" Click="OnClearClick"
                        ToolTipService.ToolTip="Clear history" Visibility="Collapsed">
                    <FontIcon Glyph="&#xE74D;" FontSize="14" />
                </Button>
            </Grid>

            <!-- Filter box -->
            <AutoSuggestBox Grid.Row="1" x:Name="FilterBox"
                            PlaceholderText="Filter by name or type..."
                            QueryIcon="Find"
                            TextChanged="OnFilterChanged"
                            Visibility="Collapsed" />

            <!-- Empty state -->
            <TextBlock Grid.Row="2" x:Name="EmptyText" Text="No recent launches"
                       Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.5" />

            <!-- Scrollable launch cards (sized to show ~2 cards) -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          MaxHeight="260">
                <StackPanel x:Name="LaunchList" Spacing="8" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
