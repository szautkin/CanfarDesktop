<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CanfarDesktop.Views.Controls.MetricBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LabelText" Grid.Column="0" Style="{StaticResource CaptionTextBlockStyle}" />
            <!-- MinWidth + right alignment keep the label column from shifting as the value text changes width -->
            <TextBlock x:Name="ValueText" Grid.Column="1" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"
                       MinWidth="120" TextAlignment="Right" />
        </Grid>
        <ProgressBar x:Name="Bar" Height="8" Maximum="100" CornerRadius="4" />
    </StackPanel>
</UserControl>
